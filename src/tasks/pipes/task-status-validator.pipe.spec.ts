import { TaskStatusValidatorPipe } from './task-status-validator.pipe';

describe('TaskStatusValidatorPipe', () => {
  it('should be defined', () => {
    expect(new TaskStatusValidatorPipe()).toBeDefined();
  });
});
